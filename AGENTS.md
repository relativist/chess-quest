# Agent Instructions

Issue tracking is not required for this project. Do not create or update project-local issue tracker records during normal work.

## Session Completion

When ending a work session:

1. Run relevant quality gates if code changed.
2. Stop local dev servers you started.
3. Note any unfinished work in the handoff.
4. Do not run git commit or git push; the user handles git publishing.
